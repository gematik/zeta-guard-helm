###############################################
# Please only temporary ignores in this file
# - also create an entry in the .vex/*.openvex.json as under_investigation
# - Keep the expiry 1 week before the next planned release
# - If it is determined that we are unaffected please make use of .vex/*.openvex.json
###############################################

# affected, medium(Red Hat): libc-bin, libc6 - glibc: glibc: Heap Buffer Overflow in `scanf` with `%mc` format specifier and...
# function of scanf family is imported by e.g. postgres libraries and executables
CVE-2026-5928 exp:2026-09-15

# affected, medium(Red Hat): libc-bin, libc6 - glibc: glibc: Information disclosure or denial of service via ungetwc function with...
# function ungetwc is loaded dynamically by libstdc++ which is loaded by postgres and others
CVE-2026-5928 exp:2026-09-15
