{{- /* vim: set ft=helm: */ -}}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: testdriver
  labels:
    app: testdriver
  {{- if .Values.hostname }}
  annotations:
    cert-manager.io/cluster-issuer: {{ .Values.clusterIssuer | default "letsencrypt-cd" | quote }}
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/hsts: "true"
    nginx.ingress.kubernetes.io/hsts-max-age: "31536000"
    nginx.ingress.kubernetes.io/hsts-include-subdomains: "true"
    nginx.ingress.kubernetes.io/hsts-preload: "true"
  {{- end }}
spec:
  ingressClassName: {{ .Values.ingressClassName | default "nginx" }}
  {{- if .Values.hostname }}
  tls:
    - hosts:
        - {{ .Values.hostname | quote }}
      secretName: "zeta-guard-tls"
  {{- end }}
  rules:
    # do not set host when working with local-cluster
    - {{- if .Values.hostname }}
      host: {{ .Values.hostname | quote }}
      {{- end }}
      http:
        paths:
          - path: /proxy
            pathType: Prefix
            backend:
              service:
                name: {{ if .Values.routeViaTigerProxy }}tiger-proxy{{ else }}testdriver{{ end }}
                port:
                  number: 80
          - path: /testdriver-api
            pathType: Prefix
            backend:
              service:
                name: testdriver
                port:
                  number: 80
