{
  "resource": "{{ .Values.pepproxy.wellKnownBase }}{{ .Values.pepproxy.wellKnownResourceSuffix }}",
  "authorization_servers": [
    "https://{{ .Values.authserver.hostname }}{{ .Values.authserver.wellKnownAuthServerPath }}"
  ],
  "zeta_asl_use": "{{ if .Values.pepproxy.asl_enabled }}required{{ else }}not_supported{{ end}}"
}
